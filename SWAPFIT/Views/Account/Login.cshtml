@model SWAPFIT.Models.NguoiDung
@{
    ViewData["Title"] = "Đăng nhập";
}

<<<<<<< HEAD
<div class="container mt-5" style="max-width: 420px;">
    <div class="card shadow-sm border-0 rounded-4">
        <div class="card-body p-4">
            <h3 class="text-center mb-4 text-primary fw-bold">Đăng nhập</h3>

            @* Hiển thị lỗi từ TempData hoặc ViewBag *@
            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger text-center">@TempData["Error"]</div>
            }
            else if (ViewBag.Error != null)
            {
                <div class="alert alert-danger text-center">@ViewBag.Error</div>
            }

            <form method="post" asp-action="Login" asp-controller="Account">
                <div class="mb-3">
                    <label asp-for="TenDangNhap" class="form-label">Tên đăng nhập</label>
                    <input asp-for="TenDangNhap" class="form-control" placeholder="Nhập tên đăng nhập" required />
                </div>

                <div class="mb-3">
                    <label asp-for="MatKhau" class="form-label">Mật khẩu</label>
                    <input asp-for="MatKhau" type="password" class="form-control" placeholder="Nhập mật khẩu" required />
                </div>

                <button type="submit" class="btn btn-primary w-100 py-2">Đăng nhập</button>
            </form>

            <p class="text-center mt-3 mb-0">
                Chưa có tài khoản?
                <a asp-action="Register" asp-controller="Account" class="text-decoration-none fw-bold">Đăng ký ngay</a>
            </p>
        </div>
    </div>
</div>
=======
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />

<style>
    body {
        background: radial-gradient(circle at top left, #e8f9f1, #f7fff9);
        min-height: 100vh;
    }

    .auth-wrapper {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .auth-card {
        border-radius: 18px;
        border: none;
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.08);
        overflow: hidden;
    }

    .auth-header {
        background: linear-gradient(135deg, #28a745, #6ad790);
        color: #fff;
        padding: 20px 24px;
        text-align: center;
    }

        .auth-header h3 {
            font-weight: 700;
            margin-bottom: 4px;
        }

        .auth-header p {
            margin: 0;
            opacity: 0.9;
            font-size: 0.95rem;
        }

    .brand-circle {
        width: 54px;
        height: 54px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.15);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 10px;
        font-size: 24px;
    }

    .auth-body {
        padding: 24px 24px 20px;
        background-color: #ffffff;
    }

    .form-label {
        font-weight: 600;
        font-size: 0.95rem;
    }

    .input-group-text {
        background-color: #f4f8f5;
        border-right: none;
    }

    .form-control {
        border-left: none;
        background-color: #f9fcfa;
    }

        .form-control:focus {
            box-shadow: none;
            border-color: #28a745;
            background-color: #ffffff;
        }

    .btn-primary-auth {
        background: #28a745;
        border-color: #28a745;
        font-weight: 600;
        letter-spacing: 0.3px;
    }

        .btn-primary-auth:hover {
            background: #208a38;
            border-color: #208a38;
        }

    .small-text {
        font-size: 0.9rem;
    }

    .link-green {
        color: #28a745;
    }

        .link-green:hover {
            color: #1f6f32;
        }

    .toggle-password {
        cursor: pointer;
    }
</style>

<div class="auth-wrapper">
    <div class="card auth-card" style="max-width: 430px; width: 100%;">
        <div class="auth-header">
            <div class="brand-circle">
                <i class="bi bi-person-circle"></i>
            </div>
            <h3>Đăng nhập SWAPFIT</h3>
            <p>Chào mừng bạn trở lại với tủ đồ xanh 🌿</p>
        </div>

        <div class="auth-body">
            @* Hiển thị lỗi từ TempData hoặc ViewBag *@
            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger text-center mb-3">@TempData["Error"]</div>
            }
            else if (ViewBag.Error != null)
            {
                <div class="alert alert-danger text-center mb-3">@ViewBag.Error</div>
            }

            <form method="post" asp-action="Login" asp-controller="Account" novalidate>
                <div class="mb-3">
                    <label asp-for="TenDangNhap" class="form-label">Tên đăng nhập</label>
                    <div class="input-group">
                        <span class="input-group-text">
                            <i class="bi bi-person"></i>
                        </span>
                        <input asp-for="TenDangNhap"
                               class="form-control"
                               placeholder="Nhập tên đăng nhập"
                               required />
                    </div>
                </div>

                <div class="mb-2">
                    <label asp-for="MatKhau" class="form-label">Mật khẩu</label>
                    <div class="input-group">
                        <span class="input-group-text">
                            <i class="bi bi-lock"></i>
                        </span>
                        <input asp-for="MatKhau"
                               type="password"
                               id="passwordInput"
                               class="form-control"
                               placeholder="Nhập mật khẩu"
                               required />
                        <span class="input-group-text toggle-password" id="togglePassword">
                            <i class="bi bi-eye-slash"></i>
                        </span>
                    </div>
                </div>

                <div class="d-flex justify-content-between align-items-center mb-3 small-text">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="RememberMe">
                        <label class="form-check-label" for="RememberMe">
                            Ghi nhớ đăng nhập
                        </label>
                    </div>
                    <a href="#" class="text-decoration-none link-green">Quên mật khẩu?</a>
                </div>

                <button type="submit" class="btn btn-primary-auth w-100 py-2 mb-2">
                    Đăng nhập
                </button>

                <p class="text-center mt-2 mb-0 small-text">
                    Chưa có tài khoản?
                    <a asp-action="Register" asp-controller="Account" class="text-decoration-none fw-semibold link-green">
                        Đăng ký ngay
                    </a>
                </p>
            </form>
        </div>
    </div>
</div>

<script>
    // Hiện / ẩn mật khẩu
    const passwordInput = document.getElementById('passwordInput');
    const togglePassword = document.getElementById('togglePassword');

    if (passwordInput && togglePassword) {
        togglePassword.addEventListener('click', function () {
            const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
            passwordInput.setAttribute('type', type);

            const icon = this.querySelector('i');
            if (type === 'password') {
                icon.classList.remove('bi-eye');
                icon.classList.add('bi-eye-slash');
            } else {
                icon.classList.remove('bi-eye-slash');
                icon.classList.add('bi-eye');
            }
        });
    }
</script>
>>>>>>> cff493713bfe5280dbb98db99eb56a2baceef7ff
