@model List<SWAPFIT.Models.DonHang>

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewData["Title"] = "Quản lý đơn hàng";
}

<div class="container mt-5">
    <h2 class="text-center mb-4 text-primary fw-bold">📦 Quản lý đơn hàng</h2>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success text-center">@TempData["Success"]</div>
    }

    <!-- Bộ lọc -->
<<<<<<< HEAD
    <form method="get" class="row g-3 mb-4">
        <div class="col-md-6">
            <div class="input-group">
                <span class="input-group-text">
                    <i class="fas fa-search"></i> <!-- Biểu tượng tìm kiếm -->
                </span>
                <input type="text" name="tuKhoa"
                       value="@(ViewBag.TuKhoa as string)"
                       class="form-control"
                       placeholder="Tìm theo mã đơn / tên khách...">
            </div>
        </div>
        <div class="col-md-3">
            <button type="submit" class="btn btn-primary w-100">Lọc</button>
        </div>
        <div class="col-md-3">
            <a asp-action="Orders" class="btn btn-outline-danger w-100">Xoá lọc</a>
        </div>
    </form>


=======
    <form method="get" class="row g-2 mb-3">
        <div class="col-md-3">
            <select name="trangThai" class="form-select">
                @{
                    string currentStatus = ViewBag.TrangThai as string ?? "ALL";
                }
                <option value="ALL" selected="@(currentStatus == "ALL")">Tất cả trạng thái</option>
                <option value="ChoXuLy" selected="@(currentStatus == "ChoXuLy")">Chờ xử lý</option>
                <option value="DangChuanBi" selected="@(currentStatus == "DangChuanBi")">Đang chuẩn bị</option>
                <option value="DangGiao" selected="@(currentStatus == "DangGiao")">Đang giao</option>
                <option value="HoanThanh" selected="@(currentStatus == "HoanThanh")">Hoàn thành</option>
                <option value="DaHuy" selected="@(currentStatus == "DaHuy")">Đã hủy</option>

            </select>
        </div>
        <div class="col-md-4">
            <input type="text" name="tuKhoa"
                   value="@(ViewBag.TuKhoa as string)"
                   class="form-control"
                   placeholder="Tìm theo mã đơn / tên khách...">
        </div>
        <div class="col-md-2">
            <button type="submit" class="btn btn-primary w-100">Lọc</button>
        </div>
        <div class="col-md-2">
            <a asp-action="Orders" class="btn btn-outline-secondary w-100">Xoá lọc</a>
        </div>
    </form>

>>>>>>> cff493713bfe5280dbb98db99eb56a2baceef7ff
    <!-- Bảng đơn hàng -->
    <div class="card shadow-sm">
        <div class="card-body p-3">
            <div class="table-responsive">
                <table class="table table-hover align-middle text-center">
                    <thead class="table-dark">
                        <tr>
                            <th>Mã đơn</th>
                            <th>Khách hàng</th>
                            <th>Ngày đặt</th>
                            <th>Tổng tiền</th>
                            <th>Trạng thái</th>
                            <th>Xem</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var d in Model)
                            {
                                <tr>
                                    <td>@d.MaDonHang</td>
                                    <td>
                                        @(d.NguoiMua?.HoTen ?? d.NguoiMua?.TenDangNhap ?? "Không rõ")
                                    </td>
                                    <td>@d.NgayDat.ToString("dd/MM/yyyy HH:mm")</td> <!-- đổi NgayDat nếu khác -->
                                    <td class="fw-bold text-success">
                                        @String.Format("{0:N0}₫", d.TongTien) <!-- đổi TongTien nếu khác -->
                                    </td>
                                    <td>
                                        <span class="badge
<<<<<<< HEAD
                                                    @(d.TrangThai == "HoanThanh" ? "bg-success" :
                                                                                            d.TrangThai == "DaHuy" ? "bg-danger" :
                                                                                            "bg-warning text-dark")">
                                    @d.TrangThai
                                </span>
                            </td>
                            <td>
                                <a asp-action="OrderDetails"
                                   asp-route-id="@d.MaDonHang"
                                   class="btn btn-sm btn-outline-primary">
                                    Chi tiết
                                </a>
                            </td>
                        </tr>
                                                }
=======
                                            @(d.TrangThai == "HoanThanh" ? "bg-success" :
                                              d.TrangThai == "DaHuy" ? "bg-danger" :
                                              "bg-warning text-dark")">
                                            @d.TrangThai
                                        </span>
                                    </td>
                                    <td>
                                        <a asp-action="OrderDetails"
                                           asp-route-id="@d.MaDonHang"
                                           class="btn btn-sm btn-outline-primary">
                                            Chi tiết
                                        </a>
                                    </td>
                                </tr>
                            }
>>>>>>> cff493713bfe5280dbb98db99eb56a2baceef7ff
                        }
                        else
                        {
                            <tr>
                                <td colspan="6" class="text-muted">Chưa có đơn hàng nào.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
